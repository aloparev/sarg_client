import lenz.htw.sarg.Move;

/**
 * min/max or alpha-beta algorithm will go here
 * it will construct the tree using <Tree> and <Node>
 */
public class Mover {
    static Move moveFinder(Board root, int pid) {
        Move move = null;
        int size = root.getStonesOfPlayerX(pid);

        return move;
    }
}
